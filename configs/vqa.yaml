vqa_root: '/kaggle/input/images-blip/Images' #followed by train2014/
train_files: ['vqa_train','vqa_val']
vg_root: ''
ann_root: 'annotation'

# set pretrained as a file path or an url
pretrained: ''

# size of vit model; base or large
vit: 'base'
batch_size_train: 4
batch_size_test: 4
vit_grad_ckpt: False
vit_ckpt_layer: 0
init_lr: 2e-5
med_config: '/kaggle/working/BLIP_VietNameseBERTMultiligual/configs/med_config.json'
image_size: 384

k_test: 128
inference: 'rank'

# optimizer
weight_decay: 0.05
min_lr: 0
max_epoch: 8
